#include <iostream>
using namespace std;

int main()
{
    // your code goes here

    int t, n, e, h, a, b, c;
    cin >> t;
    cin >> n >> e >> h >> a >> b >> c;
    int ans;

    for (int i = 0; i < t; i++)
    {
        if (a > b && a > c)
        {
            if (b > c)
            {
                int tempa = (n / e) * a;
                int tempb = (n % e)
            }
        }
    }

    return 0;
}
